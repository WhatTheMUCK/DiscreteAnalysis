#include "helpers.h"

void preprocessNodes(FILE* nodesFile, FILE* outputFile, vector<uint32_t>& nodes_id_sort);
void preprocessEdges(FILE* edgesFile, FILE* outputFile, vector<uint32_t>& nodes_id_sort);